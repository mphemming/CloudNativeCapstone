# Cloud Native Geospatial Capstone Project

## Project Goals

### Summary

Create a function that applies SQL commands to any parquet data set stored in `aodn-cloud-optimised` using DuckDB. 
Stretch goal to deploy this function to Prefect. 

